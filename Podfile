# Uncomment the next line to define a global platform for your project
platform :ios, '12.2'

target 'Hotel Reservation' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Hotel Reservation

  target 'Hotel ReservationTests' do
    inherit! :search_paths
    # Pods for testing
  end

  pod 'Firebase/Core'
  pod 'FirebaseUI'
  pod 'FirebaseUI/Auth'
  pod 'Firebase/Firestore'
  pod 'SnapKit', '~> 5.0.0'

end
